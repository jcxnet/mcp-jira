[project]
name = "mcp-jira"
version = "0.1.0"
description = "MCP stdio server exposing Jira Data Center REST v2 tools to AI agents"
requires-python = ">=3.10"
dependencies = [
    # mcp 2.x dropped FastMCP (breaking SDK rewrite); design targets FastMCP.
    "mcp>=1.0,<2",
    "httpx>=0.27",
    "rich>=13.7",
    "textual>=8.2,<9",
]

[project.scripts]
mcp-jira = "mcp_jira.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio",
    "ruff>=0.5",
    "mypy>=1.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mcp_jira"]

[tool.ruff]
target-version = "py310"
line-length = 100
# openspec/ markdown artifacts carry fenced code blocks; they are docs, not code.
exclude = ["openspec"]

[tool.mypy]
python_version = "3.10"
mypy_path = "src"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"